# Function that print hello with the argument. e.g "World"
greet() {
    echo "Hello, $1!"
}

greet "World"
# Hello, World!


# Function that display the argument pass to it
show_info() {
    echo "First arg: $1"
    echo "All args: $@"
    echo "Number of args: $#"
}

#Show the impact of local (default global)
my_function() {
    local name="Changed"
    echo "Inside: $name"
}

name="Original"
my_function
echo "Outside: $name"
# Still "Original"


#Loop on an array
for fruit in "${fruits[@]}"; do
	 echo "I like $fruit"
 done


#All together

print_numbered() {
    local items=("$@")
    local i=1
    for item in "${items[@]}"; do
        echo "$i. $item"
        ((i++))
    done
}

tasks=("Buy milk" "Walk dog" "Write code")
print_numbered "${tasks[@]}"
